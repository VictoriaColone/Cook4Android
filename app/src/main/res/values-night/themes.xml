<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Cook4Android" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/cook_primary_light</item>
        <item name="colorPrimaryVariant">@color/cook_primary</item>
        <item name="colorOnPrimary">@color/cook_on_primary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/cook_accent_light</item>
        <item name="colorSecondaryVariant">@color/cook_accent</item>
        <item name="colorOnSecondary">@color/cook_on_secondary</item>
        <!-- Background colors -->
        <item name="android:colorBackground">@color/black</item>
        <item name="colorSurface">@color/cook_surface_variant</item>
        <item name="colorOnBackground">@color/white</item>
        <item name="colorOnSurface">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowLightNavigationBar">false</item>
        <!-- Customize your theme here. -->
    </style>
</resources>